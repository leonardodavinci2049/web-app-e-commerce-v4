```shell
# remover o repositório remot
git remote -v

# Agora vou remover o repositório remoto chamado "origin":
git remote remove origin

# Vou verificar se o repositório remoto foi removido com sucesso:
git remote -v

```